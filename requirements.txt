sentence-transformers>=2.6.0
chromadb>=0.5.0
# LLM backend (default is "groq", which uses the openai client):
openai>=1.0.0
# Other optional backends:
# anthropic>=0.34.0   # only if LLM_BACKEND = "anthropic"
